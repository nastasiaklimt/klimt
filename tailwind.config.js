/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./src/**/*.{js,jsx,ts,tsx}", 
      // Add any other paths to components that use Tailwind classes
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  };
  